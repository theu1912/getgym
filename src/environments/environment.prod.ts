export const environment = {
  production: true,
  apiUrl: 'https://api.seudominio.com',   // ← troque pela URL real em produção
  adminToken: 'troque-por-um-token-seguro' // ← mesmo token do .env do backend
};
